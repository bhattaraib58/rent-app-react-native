import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  error: {
    marginHorizontal: 10,
    color: 'red',
    fontSize: 15,
    lineHeight: 20,
  },
});

export default styles;
