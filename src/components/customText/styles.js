import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  textStyle: {
    fontSize: 20,
    lineHeight: 20,
    color: '#324F80',
    fontWeight: '600',
  },
});

export default styles;
