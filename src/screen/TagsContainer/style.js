import { StyleSheet } from 'react-native';


const styles = StyleSheet.create(
    {
        tagsContainer: {
            flexDirection: "row",
            flexWrap: 'wrap',
            marginVertical: 10
        }
    }
);

export default styles;