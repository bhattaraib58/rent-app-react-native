import { StyleSheet } from 'react-native';


const styles = StyleSheet.create(
    {
        container: {
            marginTop: 10,
            marginBottom: 40,
        }
    }
);

export default styles;